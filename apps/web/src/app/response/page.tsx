import ResponseIntakeForm from '@/components/ResponseIntakeForm'

export default function ResponsePage() {
  return (
    <main>
      <h1>Urgent response intake</h1>
      <p>For time-sensitive harms that cannot wait for a fundraising cycle.</p>
      <ResponseIntakeForm />
    </main>
  )
}
